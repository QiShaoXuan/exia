{
  "name": "@exia/cli",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "bin": {
    "exia": "bin/exia.js"
  },
  "scripts": {
    "dev": "gulp",
    "build": "gulp build",
    "pub:alpha": "gulp build && npm publish --tag alpha",
    "pub:pro": "gulp build && npm publish --tag latest"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "del": "^6.0.0",
    "f2elint": "^1.2.1",
    "gulp": "^4.0.2",
    "gulp-cached": "^1.1.1",
    "gulp-eslint": "^6.0.0",
    "gulp-remember": "^1.0.1",
    "gulp-rename": "^2.0.0",
    "gulp-typescript": "^6.0.0-alpha.1",
    "gulp-uglify": "^3.0.2",
    "typescript": "^4.4.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "f2elint commit-file-scan",
      "commit-msg": "f2elint commit-msg-scan"
    }
  },
  "lint-staged": {
    "**/*.{js,ts}": "f2elint scan -i"
  }
}
